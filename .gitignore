# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Node / frontend build
node_modules/
frontend-react/dist/
frontend-react/node_modules/
*.local

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Test / lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# MLflow local tracking store
mlruns/
mlartifacts/
mlflow.db

# Derived data — regenerable with `python -m src.pipeline.run_all`.
# Raw data (data/raw) and trained artifacts (models/, artifacts/) ARE committed,
# so the API runs on a fresh clone without re-running the pipeline.
data/interim/
data/processed/
